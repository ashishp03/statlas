# learning/days/day5/exercises — Context

## Section 1: Evergreen

**What this is:** Hands-on "example-coding" exercises for the learning roadmap — toy problems that build skills, NOT the Statlas product.

**Module map:**
- `attention_from_scratch.ipynb` — Stage 3 exercise: implement `softmax`, `scaled_dot_product_attention`, `causal_mask` in numpy, with built-in ✅ test cells (verified to pass with a correct solution)

**Architecture decisions (append-only, one line each):**
- Notebooks carry self-check test cells; write the function, run, fix until green
- Exercises use toy tensors/data only — never product code or NBA data

**Non-negotiable constraints:**
- Don't import or modify `src/statlas` from these notebooks
- Don't peek at test cells for the formula before attempting

**Session log rules:** Canonical rolling session log lives in the root `CLAUDE.md`; this file stays evergreen.

## Section 2: Rolling Session Log (last 2 sessions only)

_Centralised in the root `CLAUDE.md`._
