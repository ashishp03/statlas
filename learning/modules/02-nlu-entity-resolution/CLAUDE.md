# learning/modules/02-nlu-entity-resolution — Context

## Section 1: Evergreen

**What this is:** PARKED module — NLU & entity resolution. BERT / Sentence-BERT. Ship target: an embedding-based `resolver.py` + eval set (upgrade over the v0 dictionary+fuzzy resolver in `src/statlas/entities/`).

**Module map:**
- `workbook.md` — papers/theory + blanks
- `exercises.ipynb` — graded coding exercises

**Architecture decisions (append-only, one line each):**
- Parked; the dictionary+fuzzy resolver is the current baseline to beat
- Revive when building resumes (roadmap Stage 2 covers the theory)

**Non-negotiable constraints:**
- Not the active plan; see `learning/roadmap.md`

**Session log rules:** Canonical rolling session log lives in the root `CLAUDE.md`; this file stays evergreen.

## Section 2: Rolling Session Log (last 2 sessions only)

_Centralised in the root `CLAUDE.md`._
